// Script de interação para o site (controle de navegação de links, animações, etc)
document.querySelectorAll('.profile-card').forEach(card => {
    card.addEventListener('click', () => {
        alert("Informações detalhadas sobre o criador!");
    });
});
